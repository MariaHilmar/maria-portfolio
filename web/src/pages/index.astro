---
import BaseLayout from "../layouts/BaseLayout.astro";

const skills = [
  {
    title: "Gestão de produto",
    text: "Roadmap em sprints, requisitos rastreáveis, BDD e entrega incremental.",
    evidence: "16 regras de negócio com rastreabilidade requisito → código → teste",
  },
  {
    title: "Desenvolvimento",
    text: "APIs REST async, arquitetura em camadas, Docker e CI/CD.",
    evidence: "FastAPI + SQLAlchemy 2.0 + OpenAPI",
  },
  {
    title: "Automação de testes",
    text: "Pirâmide em 5 camadas, Testcontainers e contract testing.",
    evidence: "43 testes, cobertura ~89,9%",
  },
  {
    title: "Engenharia de dados",
    text: "Pipeline ETL idempotente, migrações versionadas e integração externa.",
    evidence: "DataJud → RAG → validação → upsert",
  },
  {
    title: "Análise",
    text: "Jurimetria via agregações SQL sobre a base local.",
    evidence: "Endpoints stats por tribunal e assunto",
  },
];

const sprints = [
  { n: "01", title: "Setup", detail: "Boilerplate FastAPI, config e health check" },
  { n: "02", title: "Banco", detail: "Modelo relacional e migrations Alembic" },
  { n: "03", title: "DataJud", detail: "Cliente real, mock e pipeline de sync" },
  { n: "04", title: "Testes", detail: "Suíte em camadas e cobertura ≥ 85%" },
  { n: "05", title: "CI/CD", detail: "Lint, testes e documentação" },
];

const tribunalStats = [
  { label: "TJSP", value: 42 },
  { label: "TJRJ", value: 28 },
  { label: "TJMG", value: 18 },
  { label: "TRF3", value: 12 },
];

const assuntoStats = [
  { label: "Direito civil", value: 36 },
  { label: "Direito do consumidor", value: 24 },
  { label: "Direito trabalhista", value: 22 },
  { label: "Outros", value: 18 },
];

const maxTribunal = Math.max(...tribunalStats.map((s) => s.value));

const sjCapabilities = [
  {
    title: "Produto e requisitos",
    text: "Roadmap, priorização, BDD e alinhamento com arquitetura em domínio jurídico/dados.",
  },
  {
    title: "APIs e dados",
    text: "FastAPI, PostgreSQL/Supabase, staging vs serving, pipelines e camadas analíticas.",
  },
  {
    title: "Apps e BI",
    text: "Interfaces web (Next.js), métricas, risco e jornadas de consulta sobre base local.",
  },
  {
    title: "Segurança e LGPD",
    text: "Isolamento multi-tenant (RLS), Privacy by Design e governança no ciclo do produto.",
  },
  {
    title: "IA aplicada",
    text: "Fundamentos de ML, NLP e estratégias de busca/RAG no contexto de jurimetria.",
  },
  {
    title: "Qualidade",
    text: "Documentação viva, critérios de aceite e disciplina de testes no fluxo de entrega.",
  },
];
---

<BaseLayout>
  <header class="nav wrap">
    <a class="brand" href="#topo">Maria Hilmar</a>
    <nav aria-label="Principal">
      <a href="#sobre">Sobre</a>
      <a href="#produto">Produto</a>
      <a href="#competencias">Competências</a>
      <a href="#projetos">Projetos</a>
      <a href="#contato">Contato</a>
    </nav>
  </header>

  <main id="topo">
    <section class="hero wrap">
      <p class="hero-kicker">Portfólio · Product Manager / Product Owner · Dados & IA</p>
      <h1 class="hero-name">Maria Hilmar</h1>
      <p class="hero-tagline">
        Product Manager / Product Owner com foco em Dados e Inteligência Artificial.
        Liderança madura em squads ágeis, com projetos hands-on em Python e FastAPI na
        intersecção entre negócios e engenharia.
      </p>
      <div class="hero-cta">
        <a class="btn btn-primary" href="#produto">Produto próprio</a>
        <a class="btn btn-ghost" href="/docs/case-study-situacao-juridica">Case Situação Jurídica</a>
        <a class="btn btn-ghost" href="#projetos">Evidências públicas</a>
      </div>
      <div class="hero-plane" aria-hidden="true">
        <div class="hero-orbit"></div>
        <div class="hero-block">
          <span>Produto · privado</span>
          <strong>Situação Jurídica</strong>
        </div>
      </div>
    </section>

    <section id="sobre" class="section wrap reveal">
      <span class="section-label">Sobre</span>
      <h2>Liderança de produto na intersecção com dados e IA</h2>
      <p class="section-lead">
        Histórico consolidado em gestão de squads ágeis e governança de software, com
        forte viés técnico. MBA em Ciência de Dados (BI & Analytics). Amplitude em produto
        próprio de estudo; profundidade técnica em projetos públicos no GitHub.
      </p>
      <div class="about-grid">
        <div>
          <h3>Bagagem consolidada</h3>
          <p>Backlog orientado ao ROI, OKRs, engenharia de requisitos (BDD, UML, BPMN) e governança de QA (CTAL-TM).</p>
        </div>
        <div>
          <h3>Produto próprio (privado)</h3>
          <p>Situação Jurídica: ecossistema de jurimetria e dados. Código privado; visão e capacidades no case.</p>
        </div>
        <div>
          <h3>Como trabalho</h3>
          <p>Documentação viva, critérios de aceite e evidências verificáveis - públicas quando o código é aberto.</p>
        </div>
      </div>
    </section>

    <section id="produto" class="section wrap reveal">
      <span class="section-label">Produto próprio</span>
      <h2>Situação Jurídica</h2>
      <p class="section-lead">
        Produto próprio de estudo em jurimetria e dados jurídicos - <strong>não é vínculo
        empregatício</strong>. O código e a operação permanecem privados. Aqui está a visão
        do que o trabalho demonstra; as provas clonáveis estão na seção de projetos públicos.
      </p>

      <article class="project sj-case">
        <div class="project-main">
          <p class="privacy-note">Código privado · sem repositório público do produto</p>
          <h3>Problema</h3>
          <p>
            Dados processuais fragmentados e difíceis de auditar. É preciso ingestão,
            normalização, APIs, analytics e conformidade (LGPD) para apoiar decisões.
          </p>
          <h3>Papel</h3>
          <p>
            Product Owner / Product Lead com base técnica: roadmap, requisitos, alinhamento
            com arquitetura, qualidade e jornadas de dados.
          </p>
          <h3>Arquitetura (alto nível)</h3>
          <pre class="arch-box" aria-label="Arquitetura em alto nível">Fontes / coleta → Staging → Serving (API + banco multi-tenant)
                         → Analytics/BI · Apps web · ML / busca</pre>
          <h3>Resultados no ambiente do projeto</h3>
          <ul>
            <li>~60% menos latência após segregação staging/ETL versus serving</li>
            <li>~70% menos esforço de especificação com docs e BDD assistidos por IA</li>
            <li>Isolamento multi-tenant (RLS) e premissas Privacy by Design / LGPD</li>
          </ul>
          <div class="project-links">
            <a class="btn btn-primary" href="/docs/case-study-situacao-juridica">Case completo</a>
            <a class="btn btn-ghost" href="#projetos">Ver evidências públicas</a>
          </div>
        </div>
        <aside class="project-meta" aria-label="Fronteira de confidencialidade">
          <div>
            <strong>Privado</strong>
            <span>código do produto</span>
          </div>
          <div>
            <strong>Público</strong>
            <span>narrativa + mapa</span>
          </div>
          <div>
            <strong>Recorte</strong>
            <span>JurisSync / MGI</span>
          </div>
          <div>
            <strong>Não é</strong>
            <span>clone do produto</span>
          </div>
        </aside>
      </article>

      <h3 class="project-family">Mapa de capacidades</h3>
      <p class="project-intro">
        O que o Situação Jurídica me permite afirmar em entrevistas - sem abrir o código.
      </p>
      <ul class="capability-grid">
        {
          sjCapabilities.map((item) => (
            <li>
              <h3>{item.title}</h3>
              <p>{item.text}</p>
            </li>
          ))
        }
      </ul>
    </section>

    <section id="competencias" class="section wrap reveal">
      <span class="section-label">Competências</span>
      <h2>Evidências, não buzzwords</h2>
      <p class="section-lead">
        Amplitude no case do produto próprio; profundidade clonável em JurisSync e MGI KPI.
        Detalhamento em
        <a href="/docs/competencias">competências com evidências</a>
        e no
        <a href="/docs/case-study-situacao-juridica">case Situação Jurídica</a>.
      </p>
      <ul class="skills-list">
        {
          skills.map((skill) => (
            <li>
              <h3>{skill.title}</h3>
              <p>{skill.text}</p>
              <p class="skill-evidence">{skill.evidence}</p>
            </li>
          ))
        }
      </ul>
    </section>

    <section id="projetos" class="section wrap reveal">
      <span class="section-label">Evidências públicas</span>
      <h2>Projetos com código aberto</h2>
      <p class="section-lead">
        Recortes técnicos públicos para avaliação (clone + run). <strong>Não são o produto
        Situação Jurídica</strong> - são demonstrações independentes de API, testes, dashboard e BI.
      </p>

      <h3 class="project-family">JurisSync</h3>
      <p class="project-intro">
        Projeto de portfólio no domínio de jurimetria: API FastAPI + dashboard Next.js
        (DataJud CNJ). Avaliação local - mock ou dados reais. Não replica o ecossistema
        completo do Situação Jurídica.
      </p>
      <article class="project">
        <div class="project-main">
          <h3>O que entrega</h3>
          <ul>
            <li>Pipeline de sincronização idempotente (DataJud → validação → persistência)</li>
            <li>43 testes em 5 camadas, incluindo Testcontainers e Schemathesis</li>
            <li>Dashboard Next.js (React + TypeScript) consumindo a API</li>
            <li>Documentação viva com rastreabilidade requisito → código → teste</li>
          </ul>
          <div class="project-links">
            <a class="btn btn-primary" href="https://github.com/MariaHilmar/juris-sync" target="_blank" rel="noreferrer"
              >API</a
            >
            <a class="btn btn-ghost" href="https://github.com/MariaHilmar/juris-sync-web" target="_blank" rel="noreferrer"
              >Dashboard</a
            >
            <a class="btn btn-ghost" href="/docs/case-study-juris-sync">Case study</a>
            <a class="btn btn-ghost" href="https://github.com/MariaHilmar/juris-sync/actions/workflows/ci.yml" target="_blank" rel="noreferrer"
              >CI</a
            >
          </div>
        </div>
        <aside class="project-meta" aria-label="Métricas JurisSync">
          <div>
            <strong>43</strong>
            <span>testes</span>
          </div>
          <div>
            <strong>~90%</strong>
            <span>cobertura</span>
          </div>
          <div>
            <strong>5</strong>
            <span>camadas de teste</span>
          </div>
          <div>
            <strong>5</strong>
            <span>sprints entregues</span>
          </div>
        </aside>
      </article>

      <h3 class="project-family">MGI KPI</h3>
      <p class="project-intro">
        BI de engenharia: pipeline ETL Python (GitLab → Supabase) + dashboard Next.js somente leitura
        com KPIs, fluxo, sprint e alertas. Projeto de portfólio - não é sistema oficial do MGI.
      </p>
      <article class="project">
        <div class="project-main">
          <h3>O que entrega</h3>
          <ul>
            <li>ETL Python com regras de negócio em memória e upsert idempotente no Supabase</li>
            <li>Dashboard Next.js 16 com Server Components, cache e agregações via RPC PostgreSQL</li>
            <li>358 testes Vitest + CI com SonarCloud no dashboard</li>
            <li>Demo em produção com autenticação Supabase</li>
          </ul>
          <div class="project-links">
            <a class="btn btn-primary" href="https://github.com/MariaHilmar/mgi-kpi-dashboard" target="_blank" rel="noreferrer"
              >Dashboard</a
            >
            <a class="btn btn-ghost" href="https://github.com/MariaHilmar/mgi-kpi-pipeline" target="_blank" rel="noreferrer"
              >Pipeline</a
            >
            <a class="btn btn-ghost" href="https://web-mgi-delog.vercel.app" target="_blank" rel="noreferrer"
              >Demo live</a
            >
            <a class="btn btn-ghost" href="/docs/competencias">Evidências</a>
          </div>
        </div>
        <aside class="project-meta" aria-label="Métricas MGI KPI">
          <div>
            <strong>358</strong>
            <span>testes Vitest</span>
          </div>
          <div>
            <strong>2</strong>
            <span>repos (ETL + web)</span>
          </div>
          <div>
            <strong>RPC</strong>
            <span>agregações no Postgres</span>
          </div>
          <div>
            <strong>Live</strong>
            <span>Vercel + Supabase</span>
          </div>
        </aside>
      </article>
    </section>

    <section id="jurimetria" class="section wrap reveal">
      <span class="section-label">Análise</span>
      <h2>Jurimetria sobre a base local</h2>
      <p class="section-lead">
        Visualização ilustrativa na vitrine dos endpoints
        <code>/stats/por-tribunal</code> e <code>/stats/por-assunto</code>.
        Para o dashboard ao vivo (Next.js), clone
        <a href="https://github.com/MariaHilmar/juris-sync" target="_blank" rel="noreferrer">juris-sync</a>
        e
        <a href="https://github.com/MariaHilmar/juris-sync-web" target="_blank" rel="noreferrer">juris-sync-web</a>
        e rode local - modo mock ou DataJud real.
      </p>
      <div class="charts">
        <div class="chart-block">
          <h3>Processos por tribunal</h3>
          <ul class="bars" aria-label="Distribuição por tribunal">
            {
              tribunalStats.map((item) => (
                <li>
                  <div class="bar-label">
                    <span>{item.label}</span>
                    <span>{item.value}</span>
                  </div>
                  <div class="bar-track">
                    <div
                      class="bar-fill"
                      style={`width: ${(item.value / maxTribunal) * 100}%`}
                    />
                  </div>
                </li>
              ))
            }
          </ul>
        </div>
        <div class="chart-block">
          <h3>Processos por assunto</h3>
          <ul class="stack" aria-label="Distribuição por assunto">
            {
              assuntoStats.map((item) => (
                <li>
                  <span class="stack-swatch" style={`flex: ${item.value}`} />
                  <span class="stack-caption">{item.label} · {item.value}%</span>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </section>

    <section id="processo" class="section wrap reveal">
      <span class="section-label">Processo</span>
      <h2>Como a entrega aconteceu</h2>
      <p class="section-lead">
        Cinco sprints com qualidade como requisito: lint, testes e integração no CI a
        cada push.
      </p>
      <ol class="timeline">
        {
          sprints.map((sprint) => (
            <li>
              <span class="timeline-n">{sprint.n}</span>
              <div>
                <h3>{sprint.title}</h3>
                <p>{sprint.detail}</p>
              </div>
            </li>
          ))
        }
      </ol>
      <p class="process-note">
        CI em 3 jobs: Lint (Ruff, Black, Mypy), Test (pytest + cobertura) e Integration
        (Testcontainers + Schemathesis).
      </p>
    </section>

    <section id="contato" class="section wrap reveal">
      <span class="section-label">Contato</span>
      <h2>Vamos conversar</h2>
      <p class="section-lead">
        Disponível para conversas sobre liderança de produto, dados, IA e projetos com
        profundidade técnica.
      </p>
      <div class="contact-row">
        <a class="btn btn-primary" href="https://wa.me/5561982062117" target="_blank" rel="noreferrer"
          >WhatsApp</a
        >
        <a class="btn btn-primary" href="https://www.linkedin.com/in/mariahilmar/" target="_blank" rel="noreferrer"
          >LinkedIn</a
        >
        <a class="btn btn-primary" href="https://github.com/MariaHilmar" target="_blank" rel="noreferrer"
          >GitHub</a
        >
        <a class="btn btn-ghost" href="mailto:mariahilmar@gmail.com">E-mail</a>
        <a class="btn btn-ghost" href="/docs/case-study-juris-sync">Ler case study</a>
        <a class="btn btn-ghost" href="/docs/competencias">Ver competências</a>
      </div>
      <p class="contact-hint">
        Site live:
        <a href="https://mariahilmar-portfolio.vercel.app">mariahilmar-portfolio.vercel.app</a>
        · Perfil GitHub:
        <a href="https://github.com/MariaHilmar/MariaHilmar">MariaHilmar/MariaHilmar</a>
      </p>
    </section>
  </main>

  <footer class="footer wrap">
    <p>
      Maria Hilmar ·
      <a href="https://mariahilmar-portfolio.vercel.app">Live</a>
      · Conteúdo alinhado a <code>maria-portfolio</code>
    </p>
  </footer>
</BaseLayout>

<style>
  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.25rem 0 0.5rem;
  }

  .brand {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.05rem;
    color: var(--ink);
    text-decoration: none;
    letter-spacing: -0.02em;
  }

  .nav nav {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem 1.1rem;
  }

  .nav nav a {
    font-family: var(--font-display);
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--ink-soft);
    text-decoration: none;
  }

  .nav nav a:hover {
    color: var(--teal-deep);
  }

  .hero {
    min-height: calc(100vh - 4rem);
    display: grid;
    align-content: center;
    padding: 3rem 0 4rem;
    position: relative;
  }

  .hero-kicker {
    font-family: var(--font-display);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--teal);
    margin: 0 0 1rem;
    animation: rise 0.8s ease both;
  }

  .hero-name {
    font-family: var(--font-display);
    font-size: clamp(3.2rem, 10vw, 6.4rem);
    line-height: 0.95;
    letter-spacing: -0.04em;
    margin: 0 0 1.25rem;
    max-width: 10ch;
    animation: rise 0.9s ease 0.08s both;
  }

  .hero-tagline {
    max-width: 34rem;
    font-size: clamp(1.1rem, 2vw, 1.35rem);
    color: var(--ink-soft);
    margin: 0 0 2rem;
    animation: rise 1s ease 0.16s both;
  }

  .hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    animation: rise 1.05s ease 0.24s both;
  }

  .hero-plane {
    position: absolute;
    right: 0;
    bottom: 12%;
    width: min(340px, 42vw);
    aspect-ratio: 1;
    pointer-events: none;
  }

  .hero-orbit {
    position: absolute;
    inset: 8%;
    border: 1px solid rgba(26, 107, 110, 0.28);
    border-radius: 40% 60% 55% 45%;
    animation: drift 12s ease-in-out infinite;
    background:
      radial-gradient(circle at 30% 30%, rgba(26, 107, 110, 0.18), transparent 55%),
      radial-gradient(circle at 70% 70%, rgba(184, 149, 106, 0.16), transparent 50%);
  }

  .hero-block {
    position: absolute;
    left: 18%;
    top: 36%;
    padding: 1rem 1.15rem;
    background: rgba(247, 250, 251, 0.82);
    border: 1px solid var(--line);
    backdrop-filter: blur(6px);
    animation: floaty 5.5s ease-in-out infinite;
  }

  .hero-block span {
    display: block;
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--teal);
  }

  .hero-block strong {
    font-family: var(--font-display);
    font-size: 1.05rem;
  }

  .about-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .about-grid h3,
  .skills-list h3,
  .project h3,
  .chart-block h3,
  .timeline h3 {
    font-family: var(--font-display);
    font-size: 1.15rem;
    margin: 0 0 0.5rem;
  }

  .about-grid p,
  .skills-list p,
  .timeline p {
    margin: 0;
    color: var(--ink-soft);
  }

  .skills-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1.25rem;
  }

  .skills-list li {
    display: grid;
    grid-template-columns: 12rem 1fr 1.2fr;
    gap: 1rem;
    padding: 1.1rem 0;
    border-top: 1px solid var(--line);
  }

  .skills-list li:last-child {
    border-bottom: 1px solid var(--line);
  }

  .skill-evidence {
    font-size: 0.95rem;
    color: var(--teal-deep) !important;
    font-weight: 600;
  }

  .project-family {
    font-family: var(--font-display);
    font-size: 1.35rem;
    margin: 2.5rem 0 0.5rem;
    letter-spacing: -0.02em;
  }

  .project-family:first-of-type {
    margin-top: 0;
  }

  .project-intro {
    max-width: 42rem;
    color: var(--ink-soft);
    margin: 0 0 1.25rem;
  }

  .privacy-note {
    font-family: var(--font-display);
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--teal);
    margin: 0 0 1rem;
  }

  .arch-box {
    margin: 0 0 1.25rem;
    padding: 1rem 1.1rem;
    font-size: 0.85rem;
    line-height: 1.45;
    color: var(--ink-soft);
    background: rgba(26, 107, 110, 0.06);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    overflow-x: auto;
    white-space: pre-wrap;
  }

  .sj-case .project-main h3 {
    margin-top: 1.15rem;
  }

  .sj-case .project-main h3:first-of-type {
    margin-top: 0;
  }

  .sj-case .project-main p {
    margin: 0 0 0.25rem;
    color: var(--ink-soft);
  }

  .capability-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .capability-grid li {
    padding: 1.15rem 1.2rem;
    border: 1px solid var(--line);
    background: rgba(247, 250, 251, 0.65);
    border-radius: var(--radius);
  }

  .capability-grid h3 {
    font-family: var(--font-display);
    font-size: 1.05rem;
    margin: 0 0 0.45rem;
  }

  .capability-grid p {
    margin: 0;
    color: var(--ink-soft);
    font-size: 0.95rem;
  }

  .project + .project-family {
    margin-top: 3rem;
  }

  .project {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 1.5rem;
    align-items: stretch;
  }

  .project-main {
    padding: 1.5rem;
    border: 1px solid var(--line);
    background: rgba(247, 250, 251, 0.7);
    box-shadow: var(--shadow);
    border-radius: var(--radius);
  }

  .project-main ul {
    margin: 0 0 1.5rem;
    padding-left: 1.1rem;
    color: var(--ink-soft);
  }

  .project-main li + li {
    margin-top: 0.45rem;
  }

  .project-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem;
  }

  .project-meta {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }

  .project-meta div {
    padding: 1.1rem;
    border: 1px solid var(--line);
    background: rgba(213, 232, 233, 0.45);
    border-radius: var(--radius);
  }

  .project-meta strong {
    display: block;
    font-family: var(--font-display);
    font-size: 1.8rem;
    line-height: 1;
    margin-bottom: 0.35rem;
  }

  .project-meta span {
    color: var(--ink-soft);
    font-size: 0.92rem;
  }

  .charts {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 1.5rem;
  }

  .chart-block {
    padding: 1.35rem;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: rgba(247, 250, 251, 0.75);
  }

  .bars {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.85rem;
  }

  .bar-label {
    display: flex;
    justify-content: space-between;
    font-size: 0.92rem;
    margin-bottom: 0.3rem;
  }

  .bar-track {
    height: 10px;
    background: var(--teal-soft);
    border-radius: 999px;
    overflow: hidden;
  }

  .bar-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--teal), var(--teal-deep));
    border-radius: inherit;
    transform-origin: left;
    animation: grow 1.1s ease both;
  }

  .stack {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.75rem;
  }

  .stack li {
    display: grid;
    gap: 0.35rem;
  }

  .stack-swatch {
    display: block;
    height: 14px;
    border-radius: 999px;
    background: linear-gradient(90deg, var(--teal-soft), var(--teal), var(--sand));
  }

  .stack-caption {
    font-size: 0.92rem;
    color: var(--ink-soft);
  }

  code {
    font-size: 0.9em;
    background: rgba(26, 107, 110, 0.08);
    padding: 0.1em 0.35em;
    border-radius: 4px;
  }

  .timeline {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
  }

  .timeline li {
    display: grid;
    grid-template-columns: 3.5rem 1fr;
    gap: 1rem;
    align-items: start;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--line);
  }

  .timeline-n {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.3rem;
    color: var(--teal);
  }

  .process-note,
  .contact-hint {
    margin-top: 1.5rem;
    color: var(--ink-soft);
  }

  .contact-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .footer {
    padding: 2rem 0 3rem;
    border-top: 1px solid var(--line);
    color: var(--ink-soft);
    font-size: 0.95rem;
  }

  @keyframes rise {
    from {
      opacity: 0;
      transform: translateY(16px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes drift {
    0%,
    100% {
      transform: rotate(0deg) scale(1);
    }
    50% {
      transform: rotate(8deg) scale(1.03);
    }
  }

  @keyframes floaty {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  @keyframes grow {
    from {
      transform: scaleX(0);
    }
    to {
      transform: scaleX(1);
    }
  }

  @media (max-width: 900px) {
    .hero-plane {
      display: none;
    }

    .about-grid,
    .capability-grid,
    .project,
    .charts {
      grid-template-columns: 1fr;
    }

    .skills-list li {
      grid-template-columns: 1fr;
      gap: 0.35rem;
    }
  }

  @media (max-width: 640px) {
    .nav {
      flex-direction: column;
      align-items: flex-start;
    }

    .hero {
      min-height: auto;
      padding-top: 2rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-kicker,
    .hero-name,
    .hero-tagline,
    .hero-cta,
    .hero-orbit,
    .hero-block,
    .bar-fill {
      animation: none;
    }
  }
</style>
